# === OS / editor ===
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*.bak
*.orig

# === Python ===
__pycache__/
*.py[cod]
*.pyd

.pytest_cache/
.mypy_cache/
.ruff_cache/

.coverage
coverage/
htmlcov/

.venv/
venv/

# Packaging
*.egg-info/
dist/
build/

# === Node / Bun ===
node_modules/
.next/
.turbo/
.bun/
*.tsbuildinfo

# === Logs ===
*.log
*.log.*

# === Secrets (never commit) ===
.env
.env.*
!.env.example
!**/.env.example

autorotate/.env
autorotate/.env.*
!autorotate/.env.example

# === Keys / certificates (never commit) ===
*.pem
*.key
*.p12
*.crt
*.jks

# === Pi / Claude local tooling ===
# If you don't want to commit pi/claude workspace files, ignore them entirely.
.pi/
.claude/

# Keep a narrow allowlist if needed later (example):
# !.pi/agents/
# !.claude/commands/

# Ephemeral / tool caches
.playwright-cli/

# === Temp / generated outputs ===
tmp/
.tmp/
out/
snapshots/

# Upstream scanner repo clone + lock (generated by reality-realtlscanner-pipeline.sh)
RealiTLScanner/
.realtlscanner-pipeline.lock/

# IDEs (optional)
.vscode/
.idea/

# === Internal / keep-private (do not publish) ===
# Project meta/process notes
CLAUDE.md
FIRST_README.md
PROCESS.md
CONTINUITY.md
check

# Internal docs / plans / reviews / runbooks
/docs/

# Internal specs and extension playground (optional to publish)
/specs/
/extensions/
/execution/

# Internal test suite (optional to publish)
/autorotate/tests/

# Internal specs (keep private)
autorotate/SPEC.md

# Local task runner file (optional)
justfile
